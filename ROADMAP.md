# Rooftop Runner 版本路线图

本文件是路线图的唯一入口，只负责指向当前版本、未来候选项和历史归档。日常实现不要在这里堆积任务详情。

## 当前版本

- 版本：`0.2.0`
- 状态：`进行中`
- 当前路线图：[Roadmaps/0.2.0.md](Roadmaps/0.2.0.md)
- 版本目标：针对外部体验反馈完成中文移动端体验、竖屏原型决策、角色与楼顶环境升级、关卡独占机制和风险收益路线设计。

开始代码、资源、测试、构建配置或发布文档工作前，先阅读当前版本路线图。除非需要追溯既有决策、验收证据或提交 SHA，否则不读取归档文件。

## 未来规划

尚未纳入 `0.2.0` 的想法统一记录在 [Roadmaps/BACKLOG.md](Roadmaps/BACKLOG.md)。Backlog 不是执行清单；只有在规划后续版本或用户明确扩大范围时才读取和调整。

## 版本归档

| 版本 | 类型 | 状态 | 文件 |
| --- | --- | --- | --- |
| `0.1.0` | 版本归档 | 已完成 | [Roadmaps/Archive/0.1.0.md](Roadmaps/Archive/0.1.0.md) |
| `0.1.1` | 测试上架归档 | 已完成 | [Roadmaps/Archive/0.1.1.md](Roadmaps/Archive/0.1.1.md) |

正式发布一个版本后：

1. 在活动版本路线图记录发布日期、发布提交或 tag、最终测试、构建、签名和人工验收证据。
2. 确认所有归档记录均为 `已完成`；任何进行中、待处理、暂缓或未验收事项继续留在活动版本或 Backlog。
3. 将每条记录整理为统一的“完成日期、提交、设计目标、实现内容、验收标准、验收结果”结构，不保留“当前迭代”“最近完成”等时态标题。
4. 将整理后的版本文件移动到 `Roadmaps/Archive/<version>.md`，状态改为 `已发布`；归档文件原则上只读，仅允许修正事实错误。
5. 在本表增加归档链接，从 Backlog 选择下一版本范围并新建 `Roadmaps/<next-version>.md`。
6. 更新“当前版本”指针。没有明确的下一版本目标时，不预先展开大量任务。

## 记录规则

1. 活动版本与归档使用完全相同的记录模板，状态是两者的主要区别。
2. 开始一轮工作前，在活动版本文件写清状态、设计目标、实现内容和验收标准。
3. 实现后在验收结果中记录自动化测试、构建、人工检查和未执行项。
4. 实现或文档内容先提交为 SHA `A`；验收标准全部满足后，在活动路线图将状态改为 `已完成`，填写完成日期并将 `提交` 记为 `A`，再用后续提交保存这次路线图更新。
5. 新想法先进入 Backlog，不直接扩大活动版本。
6. 归档不得简化掉既有设计、实现或验收细节，也不得出现“当前迭代”“最近完成”等与归档状态矛盾的标题。
7. `提交` 字段始终表示该记录对应的实现或内容提交，不表示回填路线图的元数据提交；元数据提交通过 `git log -- <roadmap-file>` 追溯，禁止尝试在文件中记录自身 SHA。
8. 活动版本文件保留该版本的全部记录；版本发布前确保归档范围中的记录全部为 `已完成`，再整体移入 Archive。

## 标准记录模板

活动版本和归档中的每条记录都必须使用以下结构：

```text
## <记录名称>

- 状态：待处理 | 进行中 | 暂缓 | 已完成
- 目标版本：<version>
- 完成日期：YYYY-MM-DD | —
- 提交：<sha> | —

### 设计目标
### 实现内容
### 验收标准
### 验收结果
```

活动文件保留该版本的待处理、进行中、暂缓和已完成记录；归档文件只保留 `已完成` 记录。
