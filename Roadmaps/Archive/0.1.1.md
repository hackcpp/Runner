# Rooftop Runner 0.1.1 版本归档

- 版本状态：`已完成`
- 归档状态：`已归档`
- 归档日期：`2026-08-03`
- 版本基线提交：`a080daf`
- 发布结果：TapTap 测试版本已上架
- 工程包体版本：`0.1.1`，Android VersionCode：`2`
- Unity：`2022.3.62f1c1`
- 最终自动化：PlayMode `26/26`
- 前序归档：[0.1.0.md](0.1.0.md)

本文件只保存已经完成验收并提交的记录。CVE-2025-59489 安全升级和签名材料离机备份未伪装为完成，已保留完整记录并迁入 `0.2.0` 活动路线图。

## 1. TapTap 首次上架准备

- 状态：`已完成`
- 目标版本：`0.1.1`
- 完成日期：`2026-08-03`
- 提交：`a080daf`

### 设计目标

- 收尾 `0.1.0` 已实现内容的发布材料、包体和平台验收，不引入新玩法。
- 交付可直接提交 TapTap 的 Android Release、文案、图标、截图、视频、宣传图和隐私政策。
- 所有商店素材与实际游戏画面、应用名称和包体能力一致。
- 日常开发只构建 Mac Debug，正式打包时才构建 Android Release APK。
- 外部资质、防沉迷材料和签名材料备份状态明确，不把本轮不处理的平台硬性缺口误记为完成。

### 实现内容

- 将工程版本和 Android Version Name 同步为 `0.1.1`，VersionCode 递增为 `2`。
- 沿用用户已确认的 3 张 `1920x1080` 截图、连续实机视频和宣传图，本轮不重新生成商店视觉素材。
- 在 Android ARM64 真机对照最终素材、手势引导、安全区域、音频、发热和帧率。
- 正式提交前复核 TapTap 名称占用、页面字段、APK 包信息和签名。
- TapTap 要求的资质、ISBN 和未成年人防沉迷材料本轮不处理，保留为平台侧未完成项。
- 确认 release keystore 与 Keychain 凭据在当前构建机可用；离机备份作为未完成风险迁入 `0.2.0`。

### 验收标准

- 现有 TapTap 文案、图标、截图、视频、宣传图和隐私政策保持不变，新 APK 可直接用于技术提交。
- Android Version Name 为 `0.1.1`、VersionCode 为 `2`，应用名、包名、架构、SDK 级别和 v2 签名复核通过。
- 最终 Android Release 在至少一台 ARM64 真机完成完整流程烟测。
- 外部资质、ISBN 和防沉迷材料明确记录为本轮不处理，不纳入技术包体验收标准。
- 发布提交、APK SHA-256、测试结果和人工验收记录完整；离机备份延期决定有明确后续记录。

### 验收结果

- 已验证：Android Release 使用项目独立证书，凭据缺失时不会回退到 Debug 签名。
- 已验证：Android 应用显示名为 `天台疾跑`，包名为 `com.hackcpp.rooftoprunner`。
- 已验证：构建图标与 TapTap 上传图标使用同一画稿。
- 已验证：三关、生命 HUD、检查点恢复、终点线和通关庆祝通过人工验收。
- 已验证：隐私政策按 TapTap 模板扩充并通过 GitHub Pages 发布。
- 已验证：工程与 Android Version Name 已同步为 `0.1.1`，VersionCode 为 `2`。
- 已验证：Android ARM64 Release 构建成功；包名、应用名、API 24/35、仅 ARM64 和 v2 正式签名复核通过。
- 已验证：APK 为 `Builds/RooftopRunner-android-0.1.1.apk`，SHA-256 为 `4d2af2c76e3ba718b1cdeb11ba853bade1954c086dd37aa89dfadd15b37ad2f7`。
- 已验证：2026-08-03 最终 PlayMode `26/26` 通过，失败、跳过和不确定项均为 `0`。
- 已验证：2026-08-03 用户确认新 Android Release APK 真机流程和新手手势引导通过。
- 已验证：2026-08-03 TapTap 测试版本已成功上架。
- 用户决定：沿用已确认的 3 张 `1920x1080` 截图、视频和宣传图，本轮不重新处理。
- 用户决定：资质、ISBN 和未成年人防沉迷材料本轮不处理，仍属于 TapTap 平台侧未完成项。
- 已完成：`0.1.1` 内容提交为 `a080daf`，测试版本、APK 哈希、自动化和人工验收证据完整。
- 延期记录：release keystore 与 Keychain 凭据离机备份迁入 `0.2.0`，不得因本版本归档而丢失。

## 2. 新手手势引导真机验收

- 状态：`已完成`
- 目标版本：`0.1.1`
- 完成日期：`2026-08-03`
- 提交：`75d7927`

### 设计目标

- 初次用户能够从连续动画直接理解左、右、上、下四向滑动操作。
- 手势提示不遮挡标题、按钮、玩家、前方障碍或系统安全区域。
- 动画只在移动端开始页显示，不影响桌面流程、暂停和结算界面。

### 实现内容

- 使用程序化手指轮廓、滑动轨迹和方向箭头循环演示四向手势。
- 移除含义模糊的静态黄色触点。
- 使用不受暂停影响的时间推进，并保持无外部图片资源。
- 在 Android 真机检查实际轮廓、循环节奏和横屏安全区域。

### 验收标准

- 四个方向在一个短循环内全部展示，移动过程和方向清晰。
- 轨迹、箭头、标题、成绩和按钮之间无重叠。
- PlayMode、Mac Debug、Android Release 与 Android ARM64 真机检查均通过。

### 验收结果

- 已验证：PlayMode 覆盖显示状态、四方向循环、布局边界和静态触点移除。
- 已验证：Mac 构建和 Android Release 自动化回归通过。
- 已验证：最新正式签名 APK 已构建为 `Builds/RooftopRunner-android-0.1.1.apk`，供用户手动验证。
- 已验证：2026-08-03 用户在 Android 真机确认手指轮廓、四方向节奏和安全区域实际可读性通过。

## 3. 路线图归档规范统一

- 状态：`已完成`
- 目标版本：`0.1.1`
- 完成日期：`2026-07-31`
- 提交：`bb637b1`

### 设计目标

- 活动记录与归档记录使用完全相同的字段和章节结构，只有状态不同。
- `0.1.0` 归档只包含已完成记录，同时完整保留原有设计、实现和验收细节。
- 活动文件保留 `0.1.1` 的全部状态记录，版本发布时再整体归档已完成范围。

### 实现内容

- 定义状态、目标版本、完成日期、提交、设计目标、实现内容、验收标准和验收结果八段模板。
- 将 `0.1.0` 的完成记录整理到 `Roadmaps/Archive/0.1.0.md`。
- 将 TapTap、手势真机验收和 CVE 风险迁入 `Roadmaps/0.1.1.md`。
- 更新根入口与 AGENTS.md 的记录和归档规则。

### 验收标准

- 活动与归档的每条记录字段、顺序和章节一致。
- `0.1.0` 归档中所有记录状态均为 `已完成`，完成日期和提交 SHA 均非空。
- 归档不包含非完成事项或时态矛盾标题，也不丢失原有设计、实现、验收标准和验收结果。
- 根入口只指向 `0.1.1`，旧活动文件和失效链接为零。
- `git diff --check` 和 Markdown 相对链接检查通过。

### 验收结果

- 已验证：活动 4 条记录与归档 15 条记录均包含相同字段、顺序和四个内容章节。
- 已验证：`0.1.0` 归档记录状态全部为 `已完成`，完成日期和提交 SHA 均非空。
- 已验证：归档无非完成状态、未勾选项或时态矛盾标题，Markdown 相对链接与 `git diff --check` 通过。
- 已验证：内容提交为 `bb637b1`；本记录中的 SHA 指向内容提交，回填路线图的元数据提交由 Git 历史追溯。

## 4. Git 提交信息规范化

- 状态：`已完成`
- 目标版本：`0.1.1`
- 完成日期：`2026-07-31`
- 提交：`130d729`

### 设计目标

- 后续提交使用一致、可检索的语义化格式，能够从标题直接识别变更类型和影响范围。
- 保持提交历史简洁，不对既有提交做重写，不改变当前直推 `main` 的协作方式。

### 实现内容

- 在 `AGENTS.md` 中定义 Conventional Commits 标题格式、允许的 type、scope 命名和 description 写法。
- 规定单一逻辑关注点、路线图 SHA 回填和不兼容变更的提交方式。
- 从本记录对应的内容提交开始执行新规范。

### 验收标准

- 规则同时覆盖 `feat`、`fix`、文档、测试、构建和维护类提交。
- 标题格式、大小写、语气、长度和破坏性变更写法均有明确约束。
- 本轮内容提交及后续路线图回填提交符合新规范。
- `git diff --check` 和 Markdown 相对链接检查通过。

### 验收结果

- 已验证：规则覆盖标准 type、可选 scope、英文祈使描述、72 字符建议和不兼容变更格式。
- 已验证：内容提交 `130d729` 使用 `docs(git): define conventional commit standard`，符合新规范。
- 已验证：`git diff --check`、路线图结构和 Markdown 相对链接检查通过。
