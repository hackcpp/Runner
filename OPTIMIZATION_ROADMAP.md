# Rooftop Runner 优化台账

本文档是项目后续优化的唯一进度入口。每轮修改都应同步更新状态、验收结果和变更记录，避免实现、验证与计划脱节。

状态定义：

- `已完成`：实现已合入，并通过对应测试或人工验收。
- `进行中`：代码或内容已开始修改，但验收尚未全部完成。
- `待处理`：已确认价值和范围，尚未开始。
- `暂缓`：本阶段明确不做，防止范围失控。

## 当前基线

- 产品版本：`0.1.0`
- 目标平台：macOS 键盘操作；Android APK 构建验证
- 游戏形态：三车道三关跑酷作品集切片
- 已验证基线：PlayMode `26/26`、Mac Debug 通用二进制、签名、`1280x720` 与 `1440x900` 人工界面验收；Android IL2CPP ARM64 Release APK、包信息与 v2 正式签名验证通过
- GitHub 基线提交：`78c692e` (`Add Android touch controls and exit flow`)

## 总体进度

| 状态 | 优化主题 | 已交付内容 | 验收证据 |
| --- | --- | --- | --- |
| 已完成 | 核心角色操作 | 三车道换道、跳跃、滑铲、输入缓冲、动作中换道、自定义逻辑碰撞体 | PlayMode 角色动作测试 |
| 已完成 | 障碍与生成公平性 | 三类障碍、12 个模式、固定教学序列、三级难度、固定种子复现 | 5000 个种子、每局 1200 米完整生存路径模拟 |
| 已完成 | 计分与动作反馈 | 距离分、动作奖励、连击倍率、最佳成绩、粒子、相机反馈、独立音效 | PlayMode 计分与奖励测试 |
| 已完成 | 游戏流程与 HUD | 开始、暂停、继续、失败、重开、响应式 Canvas HUD | 两种窗口尺寸人工烟测 |
| 已完成 | 性能与代码边界 | 相机、HUD、世界池和跑局模拟拆分；视觉几何体移除 PhysX Collider | 10 分钟世界池模拟测试 |
| 已完成 | macOS 交付 | Release 构建、Universal Binary、ad-hoc 签名、TapTap 打包入口 | 构建、签名和限时启动检查 |
| 已完成 | 屋顶视觉识别 | 屋面接缝、女儿墙、屋顶设备、分层楼群、暖色窗户、黄昏灯光 | PlayMode `17/17`；Release、签名与双尺寸烟测通过 |
| 已完成 | Release 启动体验 | 将 Unity Personal 必需的启动画面改为静态同色版本 | Release、Universal Binary、签名与冷启动检查通过 |
| 已完成 | 工作区清理 | 清除旧日志、IDE 生成文件、空目录和过期发布 ZIP | 保留当前 Release、源码、缓存和未提交修改 |
| 已完成 | 角色轮廓与动作表现 | 程序化低多边形人物、状态姿态、脚步、尘雾、火花和速度拖尾 | PlayMode `18/18`；Release、签名与双尺寸烟测通过 |
| 已完成 | 背景音乐与动态编排 | 64 拍三层程序化音乐、状态交叉淡化、动作音效闪避 | PlayMode `19/19`；Release、签名、Player 烟测及耳机与扬声器试听通过 |
| 已完成 | Android 平台构建适配 | Android 构建模块、统一 APK 构建入口、平台配置与文档 | IL2CPP ARM64 APK 构建、清单和签名检查通过 |
| 已完成 | Android 触摸与真机适配 | 横屏手势、移动端 HUD、安全区域、返回键和明确退出入口 | PlayMode `22/22`、双平台 Release、桌面双尺寸与 Android ARM64 真机烟测通过 |
| 进行中 | 新手手势引导可读性 | 用手指、滑动轨迹和方向动画替换含义模糊的静态黄色圆点 | PlayMode 与双平台构建通过；待 Android 真机手动验收 |
| 进行中 | TapTap 上架材料准备 | 商店命名、定位文案、正式图标、隐私政策、素材规格与发布缺口清单 | 图标、三关文案和公开隐私政策已同步；待重录三关截图/视频、资质与正式发布 |
| 已完成 | 构建矩阵与 Android Release 正式签名 | 日常仅构建 Mac Debug；正式打包仅构建已签名 Android Release APK | PlayMode `23/23`、Mac Debug、APK v2 正式签名与包信息校验通过 |
| 已完成 | 发布材料目录隔离 | Git 忽略本地发布工作区；区分构建图标、作品集截图与商店上传导出件 | 忽略规则、跟踪状态、路径引用和媒体哈希检查通过 |
| 已完成 | 关卡制与容错跑局 | 3 个固定关卡、3 条命、碰撞确认后检查点恢复、通关流程与逐关环境 | PlayMode `26/26`、Mac Debug、签名与人工视觉验收通过 |
| 已完成 | 关卡时长与通关过渡 | 延长三关目标，手动碰撞恢复、终点线与减速庆祝 | PlayMode `26/26`、Mac Debug、Android Release、签名与人工验收通过 |

## 当前迭代：关卡时长与通关过渡

状态：`已完成`

目标：将三关目标距离调整为 `360m`、`520m`、`720m`；过终点后先进入约 `1.6s` 的减速庆祝，再显示关卡或全部完成面板；碰撞后等待玩家确认再恢复检查点；Mac Debug 可从开始界面直达指定关卡，减少人工回归耗时。

### 完成标准

- 三关新目标、检查点与固定种子路径保持可验证。
- 过终点后立即停止输入、障碍碰撞和计分，角色平滑减速，庆祝反馈只触发一次。
- 每关目标位置显示横跨三条车道的棋盘格终点线；视觉对象池化、无 Collider，且不改变逻辑终点。
- 碰撞扣命后停留在结果界面，玩家确认后才恢复检查点；不得自动重新开始。
- 庆祝阶段结束后才显示 `NEXT LEVEL` 或 `PLAY AGAIN`，不允许过终点当帧突然切换。
- Mac Debug 提供第 1、2、3 关按钮和数字键直达；Release 与移动端不显示也不响应测试入口。
- PlayMode 全量、Mac Debug Universal Binary、签名与 `1280x720` / `1440x900` 人工视觉验收通过。
- 更新 README、AGENTS.md 及本地 TapTap 上架文档；旧截图/视频标记为待重录。

### 当前验收结果

- [x] 三关目标调整为 `360m`、`520m`、`720m`，检查点随关卡长度保持在 `1/3` 与 `2/3`；5000 个固定种子完整跑局验证通过。
- [x] 终点改为 `1.6s` 减速庆祝状态；期间锁定输入、碰撞、计分与暂停入口，结果面板仅在庆祝结束后出现。
- [x] 每关目标位置增加黑白棋盘格与金色边线组成的终点标记，复用世界对象池且不参与碰撞；终点前后 `3m` 保持无障碍净空，运行时与确定性模拟共用同一规则。
- [x] 有剩余生命时碰撞进入 `LIFE LOST` 界面，点击 `CONTINUE` 或键盘确认后才恢复最近检查点。
- [x] Mac Debug 开始界面增加 `LEVEL 1`、`LEVEL 2`、`LEVEL 3` 与数字键直达入口；入口由 Development Build 和桌面平台共同限制。
- [x] `-taptapCapture` 追加第 3 关终点前 `0.5m` 到最终结算的 `1920x1080`、24 fps 连续帧，并要求清单确认实际完成关卡，用于复核和展示通关过渡。
- [x] PlayMode `26/26` 通过；Mac Debug 构建成功，产物为 `x86_64 + arm64` Universal Binary，严格签名校验通过。
- [x] Android IL2CPP ARM64 Release APK 构建成功；应用名、包名、版本、SDK 级别与 v2 正式签名通过校验，SHA-256 为 `f2722ac3682f15a23a825e792534ce29888f8259eab446e4ad77562f1ad2f221`。
- [x] README、AGENTS.md 与本地 `Distribution/TapTap/STORE_LISTING_ZH_CN.md` 已同步；现有宣传截图和视频待下次有意重录时更新。
- [x] 用户已完成人工验证，确认 `LEVEL CLEAR`、减速节奏、结果面板过渡和碰撞后手动继续符合预期。

## 当前迭代：关卡制与容错跑局

状态：`已完成`

目标：把无尽跑局改为有明确终点的三关流程；每关提供三条命和两个检查点，单次失误不再直接结束。

### 完成标准

- 三个固定距离关卡均有可验证的完整生存路径，并逐关提高起步速度、加速度、最高速度和障碍组合复杂度。
- 每关初始三条命；受击只消耗一条并等待玩家确认，再恢复到最近检查点，连击中断。
- 生命耗尽后显示关卡失败并从最近检查点重新开始；到达终点显示下一关或最终完成。
- HUD 在 `1280x720` 和 `1440x900` 清楚显示关卡、进度、生命与检查点。
- PlayMode、Mac Debug 构建、签名与启动烟测通过；按影响范围同步作品集与商店截图。

### 当前验收结果

- [x] `360m`、`520m`、`720m` 三关均定义固定 seed、目标距离和最高难度；速度逐级为 `9.4 -> 12.5`、`10.4 -> 14.5`、`11.4 -> 16.5`，并由模拟器按各关速度曲线验证完整生存路径。
- [x] 终点前继续生成可见障碍，最多延伸到终点后 `42m`；越过目标线先结算通关，不会碰撞到终点后的展示障碍。
- [x] 受击中断连击、消耗一条命并等待玩家确认，再恢复到最近检查点；恢复后保留 `16m` 无障碍反应区，三次受击后显示关卡失败，重试保留检查点并恢复三条命。
- [x] 三关分别使用蓝灰黄昏、金色夕照和紫蓝夜幕，同时逐级开放基础、中阶和高阶障碍组合。
- [x] 每关通关触发金色粒子、相机反馈、清晰的 `LEVEL CLEAR` 面板和下一关按钮；第三关显示 `ROOFTOP COMPLETE`。
- [x] HUD 已增加关卡、进度、生命、检查点信息；PlayMode `25/25`、Mac Debug Universal Binary、严格签名和启动日志通过。
- [x] 已在 Mac Debug 人工验收关卡 HUD、通关庆祝、检查点恢复、三关环境递进和第三关可读性。现有 `Portfolio/Screenshots/` 与本地 TapTap 素材不作替换，等下一轮有意更新屏幕时同步。
- [x] 实现提交：`5cac865` (`Add finite level progression`)。

## 最近完成：Android 平台构建适配

目标：在不改变现有玩法规则和 macOS 发布流程的前提下，让项目可重复生成用于真机验证的 Android APK。

### 已实现

- [x] 为 Unity `2022.3.62f1c1` 安装 Android Build Support、SDK/NDK 与 OpenJDK。
- [x] 在 `RunnerBuild.cs` 增加 Android Development、Android Release 菜单和命令行入口。
- [x] 固化 Android Bundle Identifier、版本、横屏方向、ARM64 架构和应用图标。
- [x] 实际生成 `Builds/RooftopRunner-android-0.1.0.apk` 并检查 APK 信息。
- [x] README 补充 Android 环境、界面构建和命令行构建说明。

### 验收结果

- [x] Unity 识别 Android Build Support、OpenJDK 11、NDK r23b、SDK 33/34/35 和 Build Tools 34。
- [x] Android Release 使用 IL2CPP 成功构建 `arm64-v8a` APK，文件约 `14 MB`。
- [x] APK 清单确认为包名 `com.hackcpp.rooftoprunner`、版本 `0.1.0`、最低 API 24、目标 API 35。
- [x] APK v2 调试签名校验通过；正式发布仍需配置独立 keystore。
- [x] `git diff --check` 通过，Android 构建日志和 APK 保持为本地生成物。
- [x] 实现提交：`d542333` (`Add Android build support`)。

### 完成标准

- Unity 能识别 Android 模块及随附 JDK、SDK、NDK。
- 菜单和命令行共用同一套 Android Player Settings 与构建逻辑。
- Release APK 构建成功，包名为 `com.hackcpp.rooftoprunner`，版本为 `0.1.0`，目标架构包含 ARM64。
- `git diff --check` 通过，工作区不包含日志、测试结果或 IDE 生成文件。
- 本轮只验证构建链路；触摸输入、移动端 HUD 和真机性能作为后续独立迭代。

## 最近完成：Android 触摸与真机适配

状态：`已完成`

验收边界：Android 触屏操作、响应式 HUD、构建链路和桌面 Player 自动化均已完成；2026-07-28 用户确认至少一台 Android ARM64 真机完成全流程验收。

### 已实现

- [x] 新增独立 `RunnerTouchInput`，按 DPI 阈值和主轴比例区分左、右、上、下滑动。
- [x] 手势在移动中越过阈值后立即触发，不再等待松手；放宽距离、方向与时长容差，兼顾初次操作和歧义过滤。
- [x] 触摸动作复用 `RunnerMotor` 的换道、跳跃和滑铲请求，不复制输入缓冲或动作规则。
- [x] 忽略从 UI 控件开始的触摸，避免暂停与流程按钮误触发角色动作。
- [x] HUD 内容适配 `Screen.safeArea`，并增加右上角触控暂停按钮。
- [x] 开始页使用触点与四向箭头提供无文字操作预览，教学障碍前使用“触点 + 当前方向”强化情境提示。
- [x] 开始、继续、重开按钮支持触控；Android 返回键在跑动与暂停状态间切换，在菜单和结算页退出应用。
- [x] 在开始、暂停和结算界面提供明确的 `EXIT` 按钮，统一恢复暂停状态后退出 Player。

### 验收结果

- [x] 完整 PlayMode `22/22` 通过，包含四向手势、新手容差、移动中即时提交、歧义过滤、操作提示、安全区域、明确退出入口和纯触控流程测试。
- [x] 5000 个固定种子跑局、10 分钟对象池模拟及原有键盘操作规则继续通过。
- [x] Android Release 构建成功：IL2CPP、`arm64-v8a`、最低 API 24、目标 API 35、包名与版本保持不变。
- [x] APK v2 调试签名验证通过，SHA-256 为 `20e036b502f05b3b4c4557cecfa62464664b885138894e907bee18180abb913e`。
- [x] macOS Release 为 `x86_64 + arm64` Universal Binary，签名通过；`1280x720` 与 `1440x900` 实际界面无 HUD 重叠，Player 退出正常。
- [x] 2026-07-28 用户确认至少一台 Android ARM64 真机完成安装、全流程触控、安全区域、音频、发热和帧率烟测。

### 计划范围

- 增加横屏触摸输入：点击开始、左右滑动换道、上滑跳跃、下滑滑铲。
- 为暂停、继续和重开提供可触摸控件，并处理 Android 返回键。
- 保持旧 Input API 和现有键盘操作兼容，不改变 `RunnerMotor` 的运动与碰撞规则。
- 适配刘海、挖孔和圆角屏安全区域，检查常见 `16:9`、`19.5:9` 与 `20:9` 横屏比例。
- 在 Android ARM64 真机验证开始、完整操作、失败、重开、暂停恢复、音频、发热和帧率稳定性。

### 完成标准

- 玩家只使用触屏即可完成开始、换道、跳跃、滑铲、暂停、失败和重开闭环。
- 开始、暂停和结算界面均可直接退出应用；跑动中按返回键仍先进入暂停，避免误退出。
- 四种手势互不误触，动作输入缓冲、换道队列和障碍判定与键盘版本一致。
- HUD 与触摸控件不遮挡玩家、前方障碍或系统安全区域。
- 相关 PlayMode 测试、Android Release 构建和至少一台 ARM64 真机烟测通过。

## 当前迭代：新手手势引导可读性

状态：`进行中`

问题：移动端开始页和跑动情境提示使用静态黄色圆点表示触点，但圆点缺少移动过程，初次玩家容易误认为按钮或装饰，不能直接理解为滑动操作。

### 计划范围

- [x] 移除开始页静态“圆点 + 四箭头”组合。
- [x] 移除跑动中跳跃与滑铲方向提示前的黄色圆点，只保留明确方向箭头。
- [x] 使用程序化手指形状、滑动轨迹和方向箭头，依次演示左、右、上、下四种手势。
- [x] 动画仅在移动端开始页显示，暂停、结算和桌面开始页不显示。
- [x] 使用不受暂停影响的时间推进，不增加文字操作教程或外部图片资源。
- [x] 增加 PlayMode 测试，验证显示状态、四方向循环、布局边界和静态圆点已移除。

### 当前验收结果

- [x] 开始页及跑动情境提示中的静态黄色圆点已全部移除。
- [x] 完整 PlayMode `23/23` 通过，四方向动画、显示条件、上下布局边界和素材采集路径均有断言。
- [x] macOS Release 构建、Universal Binary、严格签名和桌面双尺寸 HUD 回归检查通过。
- [x] Android Release 构建成功，保持 ARM64、最低 API 24、目标 API 35 和 APK v2 签名；SHA-256 为 `c893c1b3285ba8dd647fd7e0884f053806d8b23b7b92e486907de385648ed269`。
- [ ] Android 设备当前未连接，待真机确认手指轮廓、四方向节奏和安全区域下的实际可读性。

### 完成标准

- 初次看到开始页时，能从连续移动而非静态符号理解“滑动”操作。
- 四个方向在一个短循环内全部展示，轨迹和箭头不会与标题、成绩或按钮重叠。
- `1280x720`、`1440x900` 和 Android 横屏安全区域下布局正常。
- PlayMode、macOS Release、Android Release 与实际界面检查不回退。

## 当前发布准备：TapTap 上架材料

状态：`进行中`

评估：`难度中`，`改动规模 M`。本轮准备可审阅的商店材料；Android 应用显示名后续按页面名称统一，版本号、Bundle Identifier 和发布边界保持不变，不在 TapTap 后台直接发布。

### 计划范围

- [x] 使用已登录的 TapTap 开发者后台核对游戏名、简介、图标、截图、视频、宣传图、推荐语、封面、隐私和资质要求。
- [x] 确定一个中文主名称、两个备选名称、传播理由和正式提交前的重名复核步骤。
- [x] 编写定位、商店简介、开发者的话、首页推荐语、功能亮点、首发版本说明和社交传播文案。
- [x] 建立构建图标与 TapTap 上传图标使用同一画稿的同步规则，并同步当前技术候选。
- [x] 使用用户提供的新画稿替换当前技术候选，重新导出构建图标与 TapTap 上传图标。
- [x] 建立素材规格与发布缺口清单，区分可直接使用、需要重新制作和需要账号或资质补齐的内容。
- [x] 在 README 中加入 TapTap 上架材料入口和当前发布边界。
- [x] 生成 3 张统一为 `1920x1080` 的核心玩法截图、18 秒 H.264 实机视频和 `1920x1080` 标题宣传图。
- [ ] 重录三关版本的 3 张截图和连续实机视频，覆盖关卡/生命 HUD、检查点恢复和通关庆祝；宣传图按新画面有意更新时一并复核。
- [ ] 重新设计 TapTap 宣传图，统一屋顶透视与人物比例，避免拉伸位图和变形构图。
- [x] 增加固定种子素材采集模式，复用完整跑局生存路径自动完成换道、跳跃和滑铲，不影响普通启动。
- [x] 补充手动上传顺序，将隐私政策部署到公开 HTTPS 页面并同步 TapTap 后台链接；按 TapTap 官方模板结合 APK 实际能力补齐完整正文，不提供未公开的地址和电话。

### 当前验收结果

- [x] 已核对应用 ID `891604` 的 TapTap 未发布版本后台，并记录当前页面实际显示的文字、图片、视频和资质要求。
- [x] 文案包完成主名称 `天台疾跑`、两个备选名称、定位、简介、开发者的话、推荐语、版本说明与传播文案。
- [x] 用户提供新的 `1254x1254` RGB 方形母版；构建与 TapTap 上传 PNG 均确定性缩放为 `1024x1024`，内容及 SHA-256 完全一致，旧 SVG 候选已清理。
- [x] 新图标无透明通道、自绘圆角、文字或第三方标识；`64x64` 缩略图仍能辨认青色跑者、红色障碍和屋顶透视。
- [x] 发布清单识别出当前截图分辨率不足、Android 调试签名、ISBN、防沉迷、隐私政策和后台渠道配置等阻塞项。
- [x] README 已改为当前后台实际支持的 Android、TapTap Windows 和 Steam 渠道边界；`git diff --check`、母版与 PNG 尺寸、格式、透明通道检查通过。
- [x] 3 张截图均为 `1920x1080` 16:9 PNG 且小于 4MB；18 秒视频为 `1920x1080` H.264 MP4；宣传图为 `1920x1080` PNG，仅包含游戏标题文字且小于 4MB。
- [x] PlayMode `23/23`、macOS Universal Release、严格签名和 432 帧连续采集通过；视频首帧即为玩法，抽查 0、3、6、10、15、18 秒无黑帧、结算页或崩溃。
- [x] Android ARM64 Release 重新构建并通过 APK v2 签名校验；应用名为 `天台疾跑`，当前仍为 Android Debug 证书，SHA-256 为 `909449bfc9f9b5ea26fb9b3c915f3986bc4e4a4f3f1e068d3663e3d9473c4fb3`，仅用于真机回归。
- [x] 2026-07-29 使用用户提供的新画稿同步构建与 TapTap 图标：两个 `1024x1024` RGB PNG 内容及 SHA-256 完全一致；macOS `.icns` 与源图逐像素一致，Android APK launcher icon 为同构图的平台密度缩放版本。
- [x] 2026-07-31 公开隐私政策按 TapTap 模板扩展为十一章，明确当前 APK 不联网、不申请敏感权限、不使用 Cookie、不接入第三方 SDK，仅在设备本地保存成绩；联系渠道仅公布邮箱，并承诺 `15` 个工作日内答复。
- [ ] 候选名称正式占用复核、Android 真机画面对照、资质与发布提交仍未完成。

### 完成标准

- TapTap 文案包中的必填文字可直接粘贴，长度符合后台限制，不包含联系方式、外链、兑换码或极限宣传词。
- 正式构建图标与 TapTap 上传图标使用同一画稿，只允许格式、尺寸和平台技术属性不同；上传文件为不低于 `512x512` 的 JPG/PNG 方图，无透明底、自行切圆角、文字或第三方标识，并在 `64x64` 缩略图下保持主体可辨识。
- 截图、实机视频、宣传图、封面、LOGO、壁纸、隐私政策、正式签名、资质与防沉迷材料均有明确规格和完成状态。
- 工程与 macOS 产品名 `Rooftop Runner`、版本 `0.1.0` 和 Bundle Identifier `com.hackcpp.rooftoprunner` 保持不变；Android 应用显示名与页面统一为 `天台疾跑`。
- `git diff --check` 通过，新增图片尺寸、格式、透明通道和文件大小检查通过。

## 最近完成：发布材料目录隔离

状态：`已完成`

目标：让 Git 仓库中的构建图标、作品集截图与本地商店上传材料各自只有一个清晰用途，避免误提交或误选素材。

### 计划范围

- [x] 根目录 `Distribution/` 整体加入 Git 忽略规则，不向 GitHub 提交其中的文案、图片、视频或上传清单。
- [x] 将 TapTap 图标、截图、视频和宣传图归入明确的 `StoreUpload` 目录并使用商店用途命名。
- [x] 在 README 和本地发布说明中明确 `Assets/Brand/`、`Portfolio/Screenshots/` 与 `Distribution/` 的职责边界。
- [x] 更新发布材料文档中的全部本地路径，确认不存在旧路径引用。

### 验收结果

- [x] 根级 `/Distribution/` 忽略规则生效，`git status --ignored` 将其显示为忽略目录，`git ls-files Distribution` 结果为 `0`。
- [x] 构建脚本仍只引用 `Assets/Brand/AppIcon.png`；README 作品集预览仍只引用 `Portfolio/Screenshots/`。
- [x] TapTap 上传图标、三张截图、视频和宣传图均移入 `Distribution/TapTap/StoreUpload/`，图标可编辑源文件单独放在 `Source/`。
- [x] 旧发布素材路径引用为 `0`；移动前后图标和三张截图的 SHA-256 保持一致。
- [x] `git diff --check` 通过；本轮未改玩法、Unity 资源或构建设置，未运行 PlayMode 和 Release 构建。

### 后续同步约束

- 正式图标与 TapTap 上传图标必须使用同一画稿，只允许格式、尺寸和平台技术属性不同；更新时在同一轮同步构建图标、上传图标和可编辑源文件，并完成双平台构建图标验证。
- 可见游戏画面更新必须检查并按影响范围同步作品集截图、TapTap 截图、实机视频和宣传图。
- 本地 `Distribution/` 缺失或无法更新时必须记录未同步项，不能将相关视觉迭代标记为全部完成。

### 完成标准

- `git status --ignored` 显示整个 `Distribution/` 被根级规则忽略，且 `git ls-files Distribution` 无输出。
- `Assets/Brand/AppIcon.png` 仍是构建脚本唯一使用的图标源，`Portfolio/Screenshots/` 仍是 README 作品集预览唯一使用的截图目录。
- TapTap 上传文件的目录与文件名明确包含商店上传语义，发布文档不再引用旧 `Assets/` 路径。
- `git diff --check` 通过；本轮不改玩法、Unity 资源或构建产物，无需运行 PlayMode 和 Release 构建。

## 当前修复：TapTap Android 安全与名称一致性

状态：`进行中`

评估：`难度中`，`改动规模 S`。只升级同一条 Unity 2022.3 LTS 修复版本，并区分桌面产品名与 Android 商店显示名；不修改包名、版本号、玩法或正式签名方案。

### 计划范围

- [x] 核对 Unity 官方 CVE-2025-59489 安全公告和 2022.3 系列修复版本。
- [ ] 使用已修复的 Unity `2022.3.67f2` 打开、测试并重新构建 Android APK。
- [ ] 将 Android APK 的应用标签固定为 `天台疾跑`，macOS 产品名继续使用 `Rooftop Runner`。
- [ ] 使用 `aapt`、`apksigner` 和 APK 内 Unity 运行时信息验证名称、包名、签名与修复版本。
- [ ] 更新 README、AGENTS.md、TapTap 材料清单和上传指南中的 Unity 版本与名称说明。

### 完成标准

- Android APK 不再由受影响的 Unity `2022.3.62f1` 构建，运行时来自官方公告列出的修复版本或更新版本。
- APK `application-label` 与 TapTap 页面名称完全一致，为 `天台疾跑`。
- `com.hackcpp.rooftoprunner`、版本 `0.1.0`、VersionCode `1`、ARM64、最低 API 24 和目标 API 35 保持不变。
- PlayMode 全量测试、Android Release 构建、APK v2 签名校验与 `git diff --check` 通过。

## 最近完成：Android Release 正式签名与构建矩阵收敛

状态：`已完成`

目标：让 Android Release 构建强制使用项目独立证书，同时避免把 keystore、密码或其他敏感材料提交到 Git；收敛构建矩阵，日常开发只构建 Mac Debug，正式打包时才构建 Android Release APK。

### 计划范围

- [x] 在本地 `Distribution/Signing/` 生成项目独立 release keystore，并确认目录继续被 Git 忽略。
- [x] 使用随机强密码保护 keystore 和 key，密码只保存到 macOS Keychain。
- [x] Android Release 构建自动读取 Keychain 或专用环境变量；凭据缺失时明确失败，不回退到 Debug 签名。
- [x] 删除 Android Development、macOS Release 和 macOS ZIP 入口，只保留 Mac Debug 与 Android Release APK。
- [x] 明确日常开发只构建 Mac Debug，Android Release APK 仅在正式打包或上架前构建。
- [x] 重新构建 APK，使用 `apksigner` 核对 v2 签名、证书 DN、SHA-256 指纹和非 Debug 证书。
- [x] 更新 README、AGENTS.md、TapTap 清单和上传指南中的签名状态、备份要求与构建说明。
- [x] 重建并烟测 Mac Debug，运行完整 PlayMode 后完成清理。

### 验收结果

- Android Release APK 使用 v2 签名，证书 DN 为 `CN=Rooftop Runner, OU=Release, O=hackcpp`，证书 SHA-256 为 `3E:BC:01:D8:28:9F:5D:8C:AA:66:9F:F5:C8:AD:60:FA:6E:5D:6E:37:97:1F:D3:80:2E:C1:53:AF:2F:B8:A7:10`。
- 当前 APK SHA-256 为 `f2722ac3682f15a23a825e792534ce29888f8259eab446e4ad77562f1ad2f221`；包名、标签、版本、ARM64、最低 API 24 和目标 API 35 均通过 `aapt` 核对。
- Mac Debug 重建成功，调试器已启用，产物为 `x86_64 + arm64` Universal Binary，严格签名和启动日志检查通过。
- 完整 PlayMode `23/23` 通过；Android Development APK、Mac Release 与 Mac ZIP 入口及过期产物已清理。
- `Distribution/` 继续被 Git 整体忽略，keystore 未被跟踪；密码未写入仓库、日志或文档。
- keystore 与 Keychain 凭据的安全离机备份仍需由持有人完成；Unity `2022.3.67f2` 安全升级仍是正式上传前的独立阻塞项。

### 完成标准

- Release APK 的签名证书不再是 `CN=Android Debug`，且 APK v2 校验通过。
- release keystore、密码和 Keychain 导出均不被 Git 跟踪，仓库中不出现明文凭据。
- keystore 路径、alias、证书 SHA-256 和 APK SHA-256 有明确记录，密码不出现在日志或文档中。
- 缺失凭据时 Android Release 构建失败并给出恢复说明，不存在 Debug APK 入口或签名回退。
- 日常只构建 Mac Debug；Android Release 仅在正式打包时构建。
- 完整 PlayMode、Mac Debug、一次正式签名 Android Release 构建和 `git diff --check` 通过。

## 最近完成：背景音乐与动态编排

目标：降低短循环重复感，让菜单、常规跑动和高强度跑动具有可辨识的动态层次，同时给动作音效保留清晰空间。

### 已实现

- [x] 将单轨 32 拍循环升级为同步的氛围、节奏和高强度三层 64 拍循环，每层约 `30.48s`。
- [x] 三层在同一 DSP 时间点开始播放，只通过权重改变强度，不在重开、暂停或恢复时重新创建音频对象。
- [x] 菜单、低强度跑动、`400m+` 高强度跑动和结算使用不同层级权重，并在 `0.72s` 内交叉淡化。
- [x] 跳跃、滑铲、落地、得分和碰撞触发约 `-3dB` 闪避，攻击 `0.04s`、恢复 `0.32s`。
- [x] 合成后移除直流偏移并限制峰值，循环边界不再使用整体首尾淡出。
- [x] 为每层保留样本数、时长、峰值、直流偏移、接缝差和有限样本指标，供 PlayMode 自动验证。

### 验收结果

- [x] `git diff --check` 和 C# 编译通过。
- [x] 完整 PlayMode `19/19` 通过，新增分层结构、样本质量、交叉淡化、闪避和对象复用测试。
- [x] 5000 个固定种子跑局和 10 分钟对象池模拟继续通过。
- [x] macOS Release 构建成功，为 `x86_64 + arm64` Universal Binary，版本保持 `0.1.0`。
- [x] `codesign --verify --deep --strict` 通过。
- [x] 独立 Player 的开始、跳跃、滑铲、碰撞、重开、暂停、恢复和退出正常，Player 日志无异常。
- [x] 2026-07-27 使用耳机与扬声器各连续试听至少 5 分钟，动作音效清晰，无明显刺耳、疲劳或循环断层。

### 完成标准

- 三种运行状态听感可区分，交叉淡化和动作闪避满足量化时序。
- 三层样本峰值、直流偏移、循环接缝和有限性全部通过自动化阈值。
- 重开、暂停、恢复和 10 分钟等效运行不会新增音源或片段。
- 主观试听通过后将 P2 状态改为 `已完成`。

## 最近完成：角色轮廓与动作表现

目标：将占位胶囊升级为具有明确朝向与动作轮廓的程序化人物，同时保持动画层与逻辑碰撞完全解耦。

### 已实现

- [x] 用头、躯干、髋部、双臂、双腿和脚组成程序化低多边形人物。
- [x] 用明亮面罩、胸标、背包和背部竖条区分人物正反方向。
- [x] 增加速度关联的跑步摆臂与步频、换道侧倾、腾空伸展、落地回弹和滑铲姿态。
- [x] 增加独立脚步与落地程序化音效。
- [x] 增加复用粒子系统实现的脚步尘点、落地尘雾、滑铲火花和速度拖尾。
- [x] 使用不可见逻辑标记替换占位胶囊，全部可见部件移除 Collider。

### 验收结果

- [x] `git diff --check` 和 C# 编译通过。
- [x] 完整 PlayMode `18/18` 通过，新增姿态、脚步、粒子和逻辑碰撞隔离测试。
- [x] 5000 个固定种子跑局和 10 分钟对象池模拟继续通过。
- [x] macOS Release 构建成功，为 `x86_64 + arm64` Universal Binary。
- [x] `codesign --verify --deep --strict` 通过。
- [x] `1280x720` 与 `1440x900` 实际窗口中人物、HUD 和前方障碍无重叠。
- [x] 独立 Player 的开始、跑动、跳跃、滑铲、失败和退出正常，日志无 Unity 异常。

### 完成标准

- 静态画面能通过面罩与背部装饰读出人物朝向。
- 地面跑动、腾空和滑铲三种动作轮廓明显不同。
- 动作动画和粒子不改变 `RunnerMotor.BodyHeight`、障碍规则或固定种子生成。
- 重复效果使用固定粒子系统，角色全部可见几何体不进入 PhysX。
- PlayMode、Release、签名和双尺寸实际画面检查不回退。

## 最近完成：屋顶视觉识别

目标：隐藏标题和 HUD 后，画面仍能被直接识别为屋顶跑酷，而不是高架公路原型。

### 已实现

- [x] 用连续屋面接缝替换黄色道路虚线。
- [x] 将主路径改为屋顶板、女儿墙和压顶结构。
- [x] 在安全区域加入 HVAC、天窗和通风设备轮廓。
- [x] 将周围建筑分成近景、中景和远景高度层次。
- [x] 为背景屋顶加入水箱、空调和天线。
- [x] 增加暖色窗户、黄昏天空、三色环境光、距离雾和长阴影。
- [x] 所有新增程序化几何体继续使用对象池且不参与 PhysX。
- [x] 增加屋顶视觉结构的 PlayMode 冒烟断言。

### 验收结果

- [x] `git diff --check` 和 C# 编译通过。
- [x] 完整 PlayMode `17/17` 通过。
- [x] 10 分钟对象池模拟保持在原有上限内。
- [x] macOS Release 构建成功，为 `x86_64 + arm64` Universal Binary。
- [x] `codesign --verify --deep --strict` 通过。
- [x] `1280x720` 与 `1440x900` 实际窗口无 UI 遮挡或远景异常。
- [x] 独立 Player 启动、音乐、输入和退出正常，日志无 Unity 异常。

### 完成标准

- 不看标题也能识别屋顶环境。
- 三类障碍在最高速度下仍能通过形状快速识别。
- 屋顶设备不侵入三个可行驶车道，不产生物理碰撞体。
- 10 分钟等效跑局模拟没有对象持续增长，双尺寸实际画面没有明显闪烁或穿插。
- 现有玩法测试和构建基线不回退。

## 最近完成：Release 启动体验

目标：在 Unity Personal 许可证要求内移除 Splash Screen 的缩放动画感，并让启动背景与游戏黄昏画面自然衔接。

### 许可证边界

- Unity 版本为 `2022.3.62f1`，当前许可证为 Personal。
- Personal 必须保留 Splash Screen 和 Unity Logo，不能在项目中完全关闭。
- 本轮只使用 Unity 官方 Player Settings，不修改或绕过 Player 二进制。

### 已实现

- [x] 将 Splash Screen 动画模式从 `Dolly` 改为 `Static`。
- [x] 保留 Personal 必需的 Unity Logo，使用深色背景上的浅色样式。
- [x] 将背景色调整为与游戏黄昏天空一致的深蓝黑色。
- [x] 移除 Splash 背景图片和模糊，避免额外缩放或过渡。
- [x] 在 `RunnerBuild.ApplyPlayerSettings()` 中固化配置，确保每次 Release 构建一致。

### 验收结果

- [x] Editor 编译无错误。
- [x] macOS Release 构建成功并保持 `x86_64 + arm64` Universal Binary。
- [x] `codesign --verify --deep --strict` 通过。
- [x] Release 构建配置确认为 `Static`，不再应用 Dolly 缩放动画。
- [x] 冷启动到开始界面没有明显闪白，深色背景衔接一致。
- [x] Player 正常启动和退出，日志无 Unity 异常。

### 完成标准

- 在 Personal 许可范围内不再出现 Unity Logo 或背景缩放动画。
- Release 构建入口能够重复生成相同配置。
- 不影响应用图标、Bundle Identifier、版本号和 TapTap 打包流程。

## 后续优先级

### 估算口径

难度衡量技术不确定性、跨模块耦合和回归风险：

- `低`：沿用现有实现模式，基本不影响玩法规则。
- `中`：涉及多个运行时组件，需要新增针对性测试和人工视觉检查。
- `高`：同时影响玩法、生成、持久化或发布流程，需要完整回归验证。

改动规模包含代码、测试和文档，不包含 Unity 自动生成文件：

- `S`：1 至 2 个文件，少于约 150 行。
- `M`：2 至 4 个文件，约 150 至 400 行。
- `L`：4 至 7 个文件，约 400 至 800 行。
- `XL`：8 个以上文件或超过约 800 行，通常需要拆分迭代。

| 优先级 | 优化项 | 难度 | 改动规模 | 预计影响 | 主要风险 |
| --- | --- | --- | --- | --- | --- |
| P1 | 角色轮廓与动作表现 | 中 | L：5 至 7 个文件，约 450 至 750 行 | 角色创建、`RunnerMotor`、音效、粒子、测试 | 视觉动画误改逻辑碰撞体或动作时序 |
| P2 | 背景音乐与动态编排 | 中 | L：4 至 6 个文件，约 350 至 650 行 | `ProceduralRunnerMusic`、游戏状态、音频混合、测试、文档 | 循环接缝、峰值失真、频繁分层切换掩盖动作音效 |
| P3 | 风险收益与路线引导 | 高 | XL：7 至 9 个文件，约 650 至 1100 行 | 生成、固定种子、模拟器、计分、HUD、对象池、测试 | 奖励轨迹暗示无解路线或破坏确定性 |
| P4 | 设置与可访问性 | 中 | L：5 至 7 个文件，约 350 至 650 行 | HUD、音频、相机、持久化、游戏流程、测试 | 设置状态不同步或小分辨率布局回归 |
| P5 | 作品集交付 | 低 | M：3 至 6 个文件，约 100 至 250 行，另含媒体文件 | README、截图、视频、Release ZIP、校验值 | 媒体过大、截图过期或交付包与源码不一致 |
| P6 | 工程维护与自动化 | 高 | XL：8 个以上文件，约 1000 行以上，以迁移和配置为主 | 主控制器、生成、碰撞、测试程序集、CI、文档 | 大范围重构回归和 CI Unity License 配置失败 |
| P7 | 画面品质与宣传图对齐 | 高 | XL：8 个以上文件或资源，预计拆分为多个迭代 | 角色、屋顶模块、障碍材质、灯光、特效、相机与 Android 性能 | 实机画面仍与宣传图不一致，或画质升级导致移动端性能回退 |

### P1：角色轮廓与动作表现

状态：`已完成`

评估：`难度中`，`改动规模 L`。建议作为下一轮单独实施，先保持逻辑碰撞与动画层完全解耦。

- 将胶囊体升级为程序化低多边形人物，包含头、躯干和四肢。
- 增加跑步摆臂、换道侧倾、起跳蓄力、落地回弹和滑铲动作。
- 增加脚步节奏、落地尘雾、滑铲摩擦粒子和轻量速度拖尾。

完成标准：静态画面能读出人物朝向；三种动作轮廓明显不同；动作动画不改变逻辑碰撞规则。

当前迭代验收项：

- [x] 程序化人物包含头、躯干、髋部、双臂和双腿，并通过面罩与背部装饰明确朝向。
- [x] 地面跑动、换道、腾空和滑铲使用独立可辨识姿态，落地具有短暂回弹。
- [x] 脚步、落地尘雾、滑铲火花和速度拖尾仅在对应游戏状态触发。
- [x] 全部角色视觉部件不携带 PhysX Collider，不改变 `RunnerMotor.BodyHeight` 或障碍判定。
- [x] PlayMode、macOS Release、Universal Binary、签名与双尺寸实际画面检查通过。

### P2：背景音乐与动态编排

状态：`已完成`

评估：`难度中`，`改动规模 L`。建议作为下一轮独立实施，继续使用程序化音频，不引入外部曲库或商业素材；音乐音量持久化仍归入后续“设置与可访问性”。

- 将当前固定 `126 BPM`、32 拍单循环扩展为至少 64 拍的完整段落，减少短时间重复感。
- 保留黄昏屋顶的电子音乐方向，重做鼓组、低音、和声铺底与旋律层的频率分工，给跳跃、滑铲、得分和碰撞音效留出空间。
- 将菜单氛围、低强度跑动和高强度跑动拆成可复用层，根据游戏状态与距离难度档平滑交叉淡化，不在每次重开时重新创建 `AudioClip` 或 `AudioSource`。
- 使用相位连续或等响交叉淡化替代当前循环首尾整体淡出，消除循环点音量下陷和爆音。
- 增加动作音效触发时约 `-3dB` 的轻量音乐闪避，起效不超过 `0.04s`、恢复控制在 `0.25-0.45s`；闪避只改变音乐混合，不改变一次性音效本身。
- 将合成参数、层级权重和状态切换接口集中在 `ProceduralRunnerMusic`，`EndlessRunnerGame` 只传递菜单、跑动、暂停和结算状态及强度。

完成标准：

- 菜单、低强度跑动和高强度跑动在听感上可区分，状态变化在 `0.4-1.2s` 内平滑完成且无可闻爆音。
- 主循环不少于约 `30s`；样本绝对峰值不高于 `0.92`、直流偏移绝对值不高于 `0.001`、循环首尾样本差不高于 `0.02`，输出不削波。
- 音乐不会掩盖跳跃、滑铲、脚步、得分和碰撞音效，连续试听 5 分钟没有明显频率刺耳、节奏疲劳或循环断层。
- 重开、暂停和恢复不会新增音源或音频片段；10 分钟等效运行中音频对象数量和内存保持稳定。
- PlayMode 覆盖片段结构、样本有效性、状态切换和对象复用；macOS Release 完成人工耳机与扬声器试听。

当前迭代验收项：

- [x] 三个音乐层使用同一 64 拍时间轴并保持同步循环，主循环不少于约 30 秒。
- [x] 菜单、低强度和高强度状态在规定时间内平滑切换，暂停与恢复不重建音频对象。
- [x] 动作音效触发轻量音乐闪避，攻击和恢复时序通过自动化验证。
- [x] 自动化验证峰值、直流偏移、循环接缝、有限样本和音源复用。
- [x] 完整 PlayMode、macOS Release 与签名通过；2026-07-27 耳机和扬声器主观试听确认状态层次、循环连续性与动作音效辨识度符合预期。

### P3：风险收益与路线引导

状态：`待处理`

评估：`难度高`，`改动规模 XL`。应拆为“奖励规则与模拟”“运行时生成与池化”“HUD 与反馈”三个阶段。

- 增加金币或能量轨迹，用视觉排列提示可行路线。
- 危险路线提供更高收益，加入可控的擦身而过奖励。
- 将奖励生成纳入固定种子和完整跑局模拟。

完成标准：奖励不会暗示无解路线；同一奖励不能重复结算；固定种子输出稳定。

### P4：设置与可访问性

状态：`待处理`

评估：`难度中`，`改动规模 L`。可以分为设置数据层和设置界面两次提交，降低 UI 与持久化同时改动的风险。

- 音乐和音效独立音量。
- 相机震动开关。
- 高对比度障碍配色。
- 输入说明和清除本地成绩入口。

完成标准：设置可持久化；暂停状态可访问；关闭震动后所有撞击与落地位移反馈停止。

### P5：作品集交付

状态：`待处理`

评估：`难度低`，`改动规模 M`。代码量较少，但需要完整的截图、录制、压缩和交付包人工验收。

- 生成无系统标题栏的 1440p 16:9 截图。
- 录制 10 至 20 秒连续玩法视频或 GIF。
- README 补充设计目标、障碍求解方法和验证数字。
- 输出可直接下载的 macOS Release 压缩包及校验值。

完成标准：访客无需打开 Unity 即可理解玩法、技术亮点和完成度。

### P6：工程维护与自动化

状态：`待处理`

评估：`难度高`，`改动规模 XL`。不得一次完成，应依次拆为主控制器拆分、EditMode 测试迁移、CI 构建三个独立迭代。

- 从 `EndlessRunnerGame` 继续拆分场景生成和碰撞结算。
- 将纯 C# 跑局模拟迁移到更快的 EditMode 测试程序集。
- 评估带 Unity License 的 GitHub Actions 测试和构建流水线。

完成标准：主控制器只保留流程协调；纯逻辑测试不启动场景；发布步骤可重复执行。

### P7：画面品质与宣传图对齐

状态：`待规划`

评估：`难度高`，`改动规模 XL`。当前只记录方向，不在 TapTap 发布准备阶段扩展实现范围。正式开始前需要先确认最终宣传图，并把可实现的角色比例、材质、灯光、建筑层次和特效拆成独立验收项。

- 先制作一个视觉标杆切片，只覆盖角色、单个屋顶路段、三类障碍和黄昏灯光。
- 优先在现有 Built-in Render Pipeline 内验证材质、色彩、轮廓、阴影和相机表现，不预设迁移 URP。
- 使用实际 Player 截图与目标宣传图逐项对照，宣传素材不得表现实机无法达到的角色、场景或特效质量。
- 标杆切片通过 Android ARM64 真机性能与可读性验证后，再决定是否替换全部程序化场景资源。
- 画面层与逻辑碰撞、固定种子生成、动作时序保持解耦，不改变现有玩法不变量。

完成标准：待最终宣传图确认后补充量化指标；至少包含实机画面对照、PlayMode 回归、Android 真机帧率与发热检查，以及重新生成全部 TapTap 截图、视频和宣传图。

## 持续维护：工作区清理

### 每轮验证后清理

- 根目录 `*.log`、测试结果 XML 和临时截图日志。
- Unity 或 IDE 自动生成的 `*.csproj` 与 `*.sln`。
- `.DS_Store`、空目录和已经由本台账替代的临时计划文件。
- 不再对应当前源码的旧 APK 或 ZIP；macOS 不再生成发布 ZIP。

### 默认保留

- `Builds/RooftopRunner.app`：最近一次通过验证的本地 Debug。
- `Library`、`Temp`、`Logs`、`UserSettings`：Unity Editor 打开期间正在使用的本地状态。
- `Portfolio`、源码、测试、项目设置和所有未提交修改。

### 深度清理条件

只有在 Unity Editor 完全退出，并且需要排查导入异常或释放大量空间时，才删除 `Library`、`Temp`、`Logs` 和 `UserSettings`。删除后 Unity 会重新生成这些目录并执行完整资源导入。

## 暂缓范围

- 自由移动、屋顶缺口和攀爬系统。
- 关卡模式、成长系统和在线排行榜。
- 手柄、iOS 和 Input System 迁移。
- 与当前视觉迭代同时进行 URP 迁移。

这些项目需要重新评估核心规则或发布范围，不应混入当前作品集切片的品质迭代。

## 变更记录

| 日期 | 状态 | 内容 | 验证 | 提交 |
| --- | --- | --- | --- | --- |
| 2026-07-25 | 已完成 | 核心操作、模式公平性、反馈、HUD、对象池和 macOS 交付基线 | PlayMode `17/17`；Release 与双尺寸烟测通过 | `039c2d5` |
| 2026-07-25 | 已完成 | 屋顶视觉识别升级；建立持续优化台账 | PlayMode `17/17`；对象池模拟、Release、签名与双尺寸烟测通过 | `03d0493` |
| 2026-07-25 | 已完成 | Unity Personal 启动画面静态化与同色处理 | Release、Universal Binary、签名、冷启动与 Player 日志检查通过 | `03d0493` |
| 2026-07-25 | 已完成 | 清理旧日志、IDE 生成文件、空目录和过期 TapTap ZIP | 当前 Release、源码、Unity 活跃缓存与 Git 修改均保留 | `43431d7` |
| 2026-07-25 | 已完成 | 为 P1 至 P5 补充统一的难度和改动规模估算 | 按当前组件边界、代码行数和验证范围复核 | `86c7232` |
| 2026-07-25 | 已完成 | P1 角色轮廓与动作表现：程序化人物、状态姿态与动作效果 | PlayMode `18/18`；Release、Universal Binary、签名与双尺寸烟测通过 | `86c7232` |
| 2026-07-27 | 已完成 | P2 背景音乐与动态编排：64 拍三层循环、状态混音与动作闪避 | PlayMode `19/19`；Release、签名、Player 烟测及耳机与扬声器试听通过 | `2b865fe` |
| 2026-07-28 | 已完成 | Android 触摸与真机适配：四向手势、新手引导、触控流程、安全区域、返回键和明确退出入口 | PlayMode `22/22`；双平台 Release、签名、桌面双尺寸、退出与 Android ARM64 真机烟测通过 | `78c692e` |
| 2026-07-28 | 进行中 | 新手手势引导可读性：以手指、轨迹和四方向动画替换静态黄色圆点 | PlayMode `23/23`、双平台 Release、签名与桌面 HUD 回归通过；待 Android 真机手动验收 | `75d7927` |
| 2026-07-29 | 进行中 | TapTap 上架材料：命名文案、全新图标、3 张截图、18 秒视频、标题宣传图、上传指南和隐私草案 | PlayMode `23/23`；Release、签名、432 帧连续采集和素材规格检查通过；待 Android 对照、资质与正式发布 | `75d7927` |
| 2026-07-29 | 已完成 | 隔离本地 `Distribution/`，并区分构建图标、作品集截图和 TapTap 上传导出件 | Git 忽略与零跟踪文件、旧路径零引用、媒体 SHA-256 和 `git diff --check` 通过 | `75d7927` |
| 2026-07-29 | 已完成 | 使用用户提供的新母版统一构建图标与 TapTap 上传图标，固化同画稿规则 | 两端 PNG 内容一致；macOS Universal、签名、打包图标与 Android launcher icon、APK v2 校验通过 | `75d7927` |
| 2026-07-29 | 已完成 | 收敛为日常 Mac Debug、正式 Android Release，并完成 Android 项目独立证书签名 | PlayMode `23/23`；Mac Debug、APK v2 正式签名、包信息与 Git 忽略校验通过 | `e5afd10` |
| 2026-07-31 | 已完成 | 延长三关、增加手动碰撞恢复、关卡选择、终点线净空和减速庆祝 | PlayMode `26/26`；Mac Debug、Android Release、v2 正式签名、素材采集与人工验收通过 | `c2af47f` |
| 2026-07-31 | 已完成 | 发布《天台疾跑》公开 HTTPS 隐私政策并同步 TapTap 材料 | APK 权限、联网 API、第三方服务配置、HTML 和公开页面检查通过 | `d4a8fc4` |
| 2026-07-31 | 已完成 | 按 TapTap 官方模板扩充隐私政策，明确当前版本不适用的数据处理能力 | 十一章结构、公开 HTML 与本地备用稿、无地址和联系电话字段及 `git diff --check` 通过 | `d29f4ad` |
| 2026-07-29 | 待规划 | 记录 P7 画面品质与宣传图对齐，后续先做视觉标杆切片 | 当前不实施，不执行测试或构建 | — |

## 维护规则

1. 开始优化前，将对应条目标记为 `进行中` 并写清完成标准。
2. 实现结束后，记录自动化测试、人工检查和构建结果。
3. 只有所有完成标准满足后才能标记为 `已完成`。
4. 每次提交后，在变更记录中补充提交 SHA。
5. 新想法先进入后续优先级，不直接扩大当前迭代范围。
6. 每轮测试和构建后按“工作区清理”规则移除可再生输出。
